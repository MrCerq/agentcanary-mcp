# Smithery configuration — https://smithery.ai/docs/config
# AgentCanary MCP is retired and runs as a no-config compatibility stub.

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    additionalProperties: false
  commandFunction:
    |-
    () => ({
      command: 'npx',
      args: ['-y', 'agentcanary-mcp']
    })
